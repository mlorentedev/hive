{
  "spec": "HIVE-118-phase-c-daemon-model",
  "generated": "2026-05-31",
  "adr": "adr-011-phase-c-daemon-model (accepted 2026-05-31)",
  "gating_rule": "The agent MUST NOT set state=passing. Only the harness may, after running `verification` and capturing exit code 0. States: planned (not built) | spiked (design validated cross-OS by a passing spike; real impl + test pending) | accepted (decision/meta-criterion met) | passing (harness-only, terminal).",
  "features": [
    {
      "id": "single-owner-multi-client",
      "ac": "Single-owner daemon serves multiple clients",
      "behavior": "With `hive serve` running, >=2 concurrent client sessions complete vault reads/writes through it and exactly one hive daemon process owns the SQLite DBs + git working tree.",
      "verification": "uv run pytest tests/test_daemon.py -k two_clients_share_one_daemon",
      "state": "planned",
      "evidence": "Implemented in slice 3 as a real vault+git multi-client test: 2 concurrent `hive client` shims append to the same file through one daemon -> all 8 writes land (no lost updates) and the daemon owns the git history (9 commits = init + 2x4, one per append, deterministic across 3 runs). No new production code needed: the single-owner daemon turns the old inter-process write filelock into an intra-process threading.Lock. Pre-validated cross-OS by spike/load_spike.py (4/4 Linux+Windows). Awaiting harness pass-state."
    },
    {
      "id": "cross-session-observability",
      "ac": "Cross-session observability surface",
      "behavior": "A single call (`hive status` / `/status` / extended `worker_status`) returns metrics aggregated across >=2 sessions and persists across a session disconnect.",
      "verification": "uv run pytest tests/test_daemon.py -k status_aggregates_across_sessions",
      "state": "planned",
      "evidence": "Slice 4 built the core + primary surface: src/hive/_metrics.py (in-memory MetricsCore singleton, hot-path no-DB) fed by LifecycleMiddleware, exposed via a token-gated GET /status (ADR-011 §4 primary). Test: 2 sequential sessions call a tool then fully disconnect; /status still aggregates their per-tool counts (metrics live in the daemon, survive disconnect) and rejects a no-token request (401). Unit tests in tests/test_metrics.py cover counts/errors/percentiles/reset. Deferred: hive status CLI + worker_status block over the same core; precise concurrent-active session count (no on_disconnect hook in middleware). Awaiting harness pass-state."
    },
    {
      "id": "transparent-fallback",
      "ac": "Transparent fallback",
      "behavior": "With no daemon running, a client call still succeeds via in-process stdio and the response/health flags degraded (non-daemon) mode.",
      "verification": "uv run pytest tests/test_daemon.py -k client_falls_back",
      "state": "planned",
      "evidence": "Implemented in slice 2 (src/hive/_client.py: `hive client` shim falls back to in-process create_server() when no live daemon is reachable). Tests green locally via `make check` (exit 0): test_client_falls_back_without_daemon (no state files) + test_client_falls_back_on_stale_state (dead port behind stale state). Degraded mode surfaced via the `hive.client.mode=fallback reason={no_daemon_state,daemon_unreachable}` startup log. Awaiting harness pass-state."
    },
    {
      "id": "transport-cross-os",
      "ac": "Cross-OS transport validated",
      "behavior": "Loopback Streamable-HTTP + per-daemon bearer token round-trips a tools/call and rejects missing/wrong tokens (401) on Linux AND Windows.",
      "verification": "uv run python specs/HIVE-118-phase-c-daemon-model/spike/transport_spike.py",
      "state": "spiked",
      "evidence": "transport_spike 5/5 on Linux AND Windows (2026-05-31). To become a pytest integration test in the cross-OS CI lane."
    },
    {
      "id": "local-transport-owner-only",
      "ac": "Local transport is owner-gated by token",
      "behavior": "The token file is owner-only (POSIX 0600 / Windows icacls ACL) and a request without the matching token is refused.",
      "verification": "uv run python specs/HIVE-118-phase-c-daemon-model/spike/transport_spike.py",
      "state": "spiked",
      "evidence": "transport_spike owner-only + 401 checks pass on Linux AND Windows (2026-05-31)."
    },
    {
      "id": "write-idempotency",
      "ac": "Write idempotency across reconnect/fallback (ADR-011 §6.2)",
      "behavior": "A vault_write/patch carries an idempotency key; a retried key is a no-op (at-most-once), safe for append mode, including under concurrency.",
      "verification": "uv run pytest tests/test_idempotency.py",
      "state": "planned",
      "evidence": "Slice 2 / ADR-013 (2026-06-02): src/hive/_idempotency.py IdempotencyStore(_SqliteTracker) with is_applied (check) + claim (record) + release (pruning primitive). vault_write + vault_patch gain idempotency_key (empty default = backward compatible); the owner checks is_applied at the top of the write critical section (before retry-sensitive validation), then records the key AFTER the disk write succeeds, so a retried key short-circuits to a no-op. 5 tests in tests/test_idempotency.py: repeated key append no-op, distinct keys both apply, empty key at-least-once, patch retry no-op, 16-thread concurrent claim -> one winner. Spike-proven cross-OS 3/3 (idempotency_spike.py). make check green locally. Awaiting harness pass-state."
    },
    {
      "id": "liveness-readiness-probe",
      "ac": "Liveness + readiness probe (supporting)",
      "behavior": "The daemon serves an UNAUTHENTICATED GET /health returning 200 + {status, ready, version, uptime_s}; readiness reflects vault resolvability; the token-gated per-tool metrics + budget on /status are never leaked to a no-token caller.",
      "verification": "uv run pytest tests/test_daemon.py -k health_probe",
      "state": "planned",
      "evidence": "Slice 1.1 (2026-05-31): src/hive/server.py _health_payload + unauthenticated /health route. Liveness (200 + status=ok) vs readiness (ready = vault working tree resolvable) on one endpoint; sensitive metrics/budget stay token-gated on /status. The foundation the supervised systemd unit (readiness check), restart-on-upgrade (wait-for-ready), and the client reconnect probe consume. Test green locally via make check (659 passed). Awaiting harness pass-state.",
      "supports": "supervised-recovery"
    },
    {
      "id": "startup-self-heal",
      "ac": "Startup self-heal + single-owner guard",
      "behavior": "On startup the daemon acquires a singleton advisory lock on <state_dir>/daemon.lock; a second `hive serve` -- even on a different auto-port -- declines cleanly (exit 0) instead of double-owning the SQLite DBs + git tree. Holding that lock proves single ownership, so the daemon then clears a stale `.git/index.lock` left by a prior unclean exit (a lock naming a live PID is spared), letting writes commit again.",
      "verification": "uv run pytest tests/test_daemon.py -k \"self_heals_stale_index_lock or declines_when_state_owned\"",
      "state": "planned",
      "evidence": "Slice 1.2 (2026-06-01): src/hive/_daemon.py _acquire_singleton_lock (filelock, non-blocking timeout=0, kernel-released on crash so a supervised restart re-acquires) + _startup_self_heal (clears a stale .git/index.lock under the proven single-owner invariant; _index_lock_is_live spares a live external committer). Closes the auto-port single-owner gap (_free_port lets two daemons bind different ports). Two TDD tests green locally via make check (661 passed, mypy --strict + ruff clean): a planted dead-PID index.lock is cleared and the next write commits; a second daemon on a different port declines exit 0 without binding. Awaiting harness pass-state.",
      "supports": "supervised-recovery"
    },
    {
      "id": "supervised-recovery",
      "ac": "Supervised recovery",
      "behavior": "Killing the daemon (SIGKILL) triggers supervisor auto-restart; durable state (SQLite + git) is intact, and in-flight clients reconnect or fall back.",
      "verification": "pytest tests/test_daemon_recovery.py -k sigkill_restart",
      "state": "planned",
      "evidence": "Crash-only durability pre-validated by spike/resilience_spike.py 4/4 (Linux+Windows): state durable + integrity_check=ok after SIGKILL/TerminateProcess, reconnect, disconnect survival. The supervisor (systemd/launchd unit) itself is post-build."
    },
    {
      "id": "post-mortem-artifact",
      "ac": "Post-mortem artifact on crash",
      "behavior": "An abnormal exit writes a crash artifact (last-N ring buffer, lock/DB state, in-flight requests, version, traceback) with NO secrets/API keys (ADR-011 §6.3 redaction).",
      "verification": "pytest tests/test_daemon_postmortem.py -k crash_artifact_no_secrets",
      "state": "planned",
      "evidence": null
    },
    {
      "id": "correlated-structured-logging",
      "ac": "Correlated, structured logging",
      "behavior": "Every request is logged once with a correlation_id + session_id traceable across the daemon log and metrics; one daemon log replaces per-PID files.",
      "verification": "pytest tests/test_daemon_logging.py -k correlation_id",
      "state": "planned",
      "evidence": null
    },
    {
      "id": "three-plane-telemetry",
      "ac": "Three-plane telemetry separation",
      "behavior": "Live metrics served from memory (no synchronous per-call DB write on the hot path); forensic JSON-lines + crash artifact; historical telemetry in usage.db surviving a restart.",
      "verification": "pytest tests/test_daemon_telemetry.py -k three_plane",
      "state": "planned",
      "evidence": null
    },
    {
      "id": "transport-robustness",
      "ac": "Transport/auth robustness (supporting)",
      "behavior": "A large (1 MB) payload round-trips intact; missing/empty/wrong-scheme auth are all rejected; a port-in-use daemon exits cleanly.",
      "verification": "uv run python specs/HIVE-118-phase-c-daemon-model/spike/robustness_spike.py",
      "state": "spiked",
      "evidence": "robustness_spike 3/3 on Linux AND Windows (2026-05-31)."
    },
    {
      "id": "adr-011-accepted",
      "ac": "ADR-011 written and merged",
      "behavior": "ADR-011 captures the daemon decision, transport choice, fallback contract, resilience/observability design, and what it supersedes in ADR-005.",
      "verification": "grep -q '^status: accepted' docs/adr/adr-011-phase-c-daemon-model.md",
      "state": "accepted",
      "evidence": "ADR-011 status: accepted (2026-05-31). Merges with the HIVE-118 PR."
    }
  ]
}
