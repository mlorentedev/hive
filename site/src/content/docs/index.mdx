---
title: On-demand Obsidian vault access for AI coding assistants
description: MCP server that connects AI coding assistants to Obsidian vaults — query context, search decisions, capture lessons. 67–82% token reduction.
template: splash
hero:
  tagline: On-demand Obsidian vault access + worker delegation for any MCP client.
  actions:
    - text: Get Started
      link: /hive/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/mlorentedev/hive
      variant: minimal
      icon: external
---

import { Card, CardGrid, Tabs, TabItem } from '@astrojs/starlight/components';

## Why Hive?

Every session starts from scratch.

Your AI assistant doesn't remember that you chose PostgreSQL over MySQL last month. Or that your deploy pipeline has a quirk with Redis cache. You end up repeating yourself, pasting the same context, stuffing everything into a giant CLAUDE.md that eats thousands of tokens before you even ask a question.

Hive fixes this.

You keep a folder of Markdown files — project decisions, architecture notes, lessons learned, task lists. Hive lets your assistant query them on demand. Only what's relevant, when it's relevant.

But the real power isn't per-project memory.

It's the shared practices. You define your coding standards, architecture patterns, and review checklists once. Every project — personal side projects, your day job, your team's repos — follows the same playbook. That consistency compounds. And it's what actually makes you faster.

One more thing: this doesn't have to be an Obsidian vault. Any folder with `.md` files works. **Your knowledge lives in plain files you control, not locked inside a tool.**

## Features

<CardGrid>
  <Card title="10 Tools" icon="puzzle">
    Query, search, write, patch, and list vault content. Delegate tasks to cheaper models. Track health and budget.
  </Card>
  <Card title="5 Resources" icon="document">
    MCP resources for projects, health metrics, context, tasks, and lessons — directly consumable by AI clients.
  </Card>
  <Card title="4 Prompts" icon="pencil">
    Built-in protocols for retrospectives, delegation, vault sync, and benchmarking.
  </Card>
  <Card title="Worker Routing" icon="rocket">
    Automatic tier selection: local Ollama, OpenRouter free, OpenRouter paid, or reject. $1/mo budget cap.
  </Card>
</CardGrid>

## Quick Install

<Tabs syncKey="client">
  <TabItem label="Claude Code">
    ```bash
    claude mcp add -s user hive -- uvx --upgrade hive-vault
    ```
  </TabItem>
  <TabItem label="Codex CLI">
    Add to `~/.codex/config.toml`:
    ```toml
    [mcp_servers.hive-vault]
    command = "uvx"
    args = ["--upgrade", "hive-vault"]
    ```
  </TabItem>
  <TabItem label="Gemini CLI">
    ```bash
    gemini mcp add -s user hive-vault uvx -- --upgrade hive-vault
    ```
  </TabItem>
  <TabItem label="Copilot (VS Code)">
    Add to `.vscode/mcp.json`:
    ```json
    {
      "servers": {
        "hive-vault": {
          "command": "uvx",
          "args": ["--upgrade", "hive-vault"]
        }
      }
    }
    ```
  </TabItem>
  <TabItem label="OpenCode">
    Add to `~/.config/opencode/opencode.jsonc`:
    ```jsonc
    {
      "mcp": {
        "hive": {
          "type": "local",
          "command": ["uvx", "--upgrade", "hive-vault"],
          "enabled": true
        }
      }
    }
    ```
  </TabItem>
</Tabs>

No cloning, no venv, no setup. [Learn more →](/hive/getting-started/)

## Recommended Workflow

The highest-value setup:

1. **[Obsidian](https://obsidian.md/)** — local-first knowledge base, Markdown native, 1M+ community
2. **[Obsidian Git](https://github.com/Vinzent03/obsidian-git)** — auto-commits vault changes on a schedule (5–10 min recommended)
3. **Hive** — bridges your vault to any AI coding assistant via MCP

Your assistant writes to the vault → Obsidian Git auto-commits → next session, everything is there. No manual sync. For write-heavy flows, pair `vault_write(commit=False)` with the plugin — Hive defers each commit, the plugin batches them; see [Configuration → Recommended configuration](/configuration/#recommended-configuration).

> Hive works with any Markdown directory — Obsidian is recommended, not required.

## Guides

- [Benchmarks](/hive/guides/benchmarks/) — token savings data and `max_lines` calibration.
